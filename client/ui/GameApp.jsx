/**
 * The root of the app
 */
const React = require('react');

var GameApp = React.createClass({
    render() {
        return (
            <div>
                Hello World!
            </div>
        );
    }
});

module.exports = GameApp;